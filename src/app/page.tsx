// =====================================================================
// MARKET1 OS ROOT PAGE ENTRY (NEXT.JS APP ROUTER)
// Bridges the root route '/' to the main App component.
// =====================================================================

'use client';

import React from 'react';
import App from '../App';

export default function Page() {
  return <App />;
}
